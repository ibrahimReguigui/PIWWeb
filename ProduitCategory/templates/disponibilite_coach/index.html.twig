{% extends 'base-back.html.twig' %}

{% block title %}DisponibiliteCoach index{% endblock %}

{% block body %}
    <h1>DisponibiliteCoach index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Time</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disponibilite_coach in disponibilite_coaches %}
            <tr>
                <td>{{ disponibilite_coach.id }}</td>
                <td>{{ disponibilite_coach.date ? disponibilite_coach.date|date('Y-m-d') : '' }}</td>
                <td>{{ disponibilite_coach.time ? disponibilite_coach.time|date('H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_disponibilite_coach_show', {'id': disponibilite_coach.id}) }}">show</a>
                    <a href="{{ path('app_disponibilite_coach_edit', {'id': disponibilite_coach.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_disponibilite_coach_new') }}">Create new</a>
{% endblock %}
