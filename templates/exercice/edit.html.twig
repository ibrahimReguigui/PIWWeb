{% extends 'base-back.html.twig' %}

{% block title %}Edit Exercice{% endblock %}

{% block body %}

    <h1 style="color: brown;" >Edit Exercice</h1>

    {{ include('exercice/_form.html.twig', {'button_label': 'Update'}) }}    

    <a  class="btn btn-success"  href="{{ path('app_exercice_index') }}">back to list</a>

    {{ include('exercice/_delete_form.html.twig') }}

{% endblock %}
