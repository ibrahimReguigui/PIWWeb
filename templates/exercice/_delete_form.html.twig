<form method="post" action="{{ path('app_exercice_delete', {'id': exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.id) }}">
    <button  style="color: red;"="btn">Delete</button>
</form>
