<form method="post" action="{{ path('app_abonements_delete', {'id': abonement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonement.id) }}">
    <button class="btn">Delete</button>
</form>
