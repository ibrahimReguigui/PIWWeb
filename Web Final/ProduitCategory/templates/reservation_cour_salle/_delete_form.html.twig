<form method="post" action="{{ path('app_reservation_cour_salle_delete', {'id': reservation_cour_salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_cour_salle.id) }}">
    <button class="btn">Delete</button>
</form>
