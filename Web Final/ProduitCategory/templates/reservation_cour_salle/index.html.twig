{% extends 'front/fresh-base.html.twig' %}

{% block title %}ReservationCourSalle index{% endblock %}

{% block body %}
    <h1>ReservationCourSalle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation_cour_salle in reservation_cour_salles %}
            <tr>
                <td>{{ reservation_cour_salle.id }}</td>
                <td>
                    <a href="{{ path('app_reservation_cour_salle_show', {'id': reservation_cour_salle.id}) }}">show</a>
                    <a href="{{ path('app_reservation_cour_salle_edit', {'id': reservation_cour_salle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservation_cour_salle_new') }}">Create new</a>
{% endblock %}
